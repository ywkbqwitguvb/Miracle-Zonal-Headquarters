# Miracle Zonal Headquarters — static site

Simple responsive HTML/CSS/JS site scaffold.

Preview

- Open `index.html` in your browser, or run a local server:

```bash
# Python 3
python -m http.server 8000

# then open http://localhost:8000
```

Files

- `index.html`
- `css/style.css`
- `js/main.js`

Next steps: customize copy, images, and deploy to your hosting (Netlify/Vercel/GitHub Pages).
